name: Docker Build and Push

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  pull_request:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push Docker images
        run: |
          echo "Add your build and push commands here"
          # Example:
          # docker build -f Dockerfile -t ghcr.io/${{ github.repository }}:latest .
          # docker build -f Dockerfile.arm64 -t ghcr.io/${{ github.repository }}:latest-arm64 .
          # docker push ghcr.io/${{ github.repository }}:latest
          # docker push ghcr.io/${{ github.repository }}:latest-arm64

